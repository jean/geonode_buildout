{% with auth_method = config.postgres_version < '8.4' and 'ident sameuser' or 'ident' %}
### Admin
local   all         ${config.postgres_admin_name}              trust

### Superusers
local   all         &{config.postgres_superuser_name}          ${auth_method}

local   all         all                               ${auth_method}
host    all         all         127.0.0.1/32          md5
host    all         all         ::1/128               md5
{% end %}
