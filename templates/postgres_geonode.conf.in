# Our settings, add more here as needed

data_directory = '${config:postgres_data_dir}'		# use data in another directory
unix_socket_directories = '${config:postgres_socket_dir}'
hba_file = '${config:postgres_data_dir}/pg_hba.conf'	# host-based authentication file
port = ${config:postgres_port}				# (change requires restart)
